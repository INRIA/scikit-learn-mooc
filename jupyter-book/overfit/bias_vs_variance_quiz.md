# ✅ Quiz

```{admonition} Question
Choosing a model with a high bias:

- leads to underfitting?
- leads to overfitting?
- increases the sensitivity of the learned prediction function to a random resampling of the training set observations?
- leads the learned prediction function to make systematic errors?
```

```{admonition} Question
Choosing a model with a high variance:

- leads to underfitting?
- leads to overfitting?
- increases the sensitivity of the learned prediction function to a random resampling of the training set observations?
- leads the learned prediction function to make systematic errors?
```
